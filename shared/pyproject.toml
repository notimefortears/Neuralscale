[build-system]
requires = ["setuptools>=69.0"]
build-backend = "setuptools.build_meta"

[project]
name = "neuralscale-shared"
version = "0.1.0"
description = "Shared utilities for NeuralScale microservices"
requires-python = ">=3.11"
dependencies = []
